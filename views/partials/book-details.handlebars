<section class="card">
  <a href="/book/{{id}}">
    <header>{{title}}</header>

    <div class="card-body">
      {{!-- uncomment when we can pull pictures --}}
      {{!-- <img src="/images/{{filename}}" alt="{{description}}"> --}}
      <p>By: {{author}}</p>
    </div>
  </a>
</section>

{{!-- no description yet  --}}